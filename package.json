{
  "name": "w3-chainalysis-action",
  "version": "0.1.0",
  "private": true,
  "description": "Chainalysis sanctions screening and compliance for W3 workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  }
}
